# 24shoots Political Booking Website

Flashoot-style landing + instant pay-and-book flow for **political leaders only** (GHMC Hyderabad).

## Run locally
1) `npm install`
2) Copy `.env.example` → `.env.local` and fill values
3) `npm run dev`

## Routes
- `/` Home
- `/pricing` Pricing
- `/book` Booking + Razorpay checkout
- `/admin/login` Admin login
- `/admin` Admin dashboard

## Environment variables
See `.env.example`
